{
  "name": "dev.beautyincode.se",
  "version": "0.2.0",
  "description": "Dev web site for Beauty In Code",
  "main": "index.html",
  "scripts": {
    "prestart": "yarn",
    "start": "concurrently 'yarn run watch' 'yarn run jekyll:serve'",
    "prebuild": "yarn run dist:clean",
    "watch": "yarn run watch:css && yarn run watch:svg && yarn run watch:js",
    "watch:css": "onchange ./css/* -i -- yarn run css",
    "watch:svg": "onchange ./src/svg/* -i -- yarn run svg",
    "watch:s": "onchange ./js/* -i -- yarn run js",
    "css": "uglifycss ./css/*.css --output ./_includes/styles.css",
    "svg": "svg-sprite -C svg-sprite.config.json --dest ./_includes svg/*.svg",
    "js": "uglifyjs ./js/main.js --output ./_includes/main.js",
    "jekyll:serve": "sleep 1; jekyll serve --incremental",
    "jekyll:build": "jekyll build --incremental"
  },
  "author": "Fredrik Frodlund",
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^4.1.0",
    "onchange": "^5.2.0",
    "svg-sprite": "^1.3.7",
    "uglify-es": "^3.0.28",
    "uglifycss": "^0.0.29",
    "yarn": "^1.17.3"
  }
}
